import { SxProps, Theme } from '@mui/material';
import { FONT } from '@/constants/font';

export const routeDateStyles: SxProps<Theme> = {
  marginBottom: '30px',
  paddingTop: '20px',
  fontSize: FONT.fontSize.extraLarge,
};

export const routeIdStyles: SxProps<Theme> = {
  marginBottom: '30px',
  fontSize: FONT.fontSize.extraLarge,
};

export const driverTitleStyles: SxProps<Theme> = {
  marginBottom: '30px',
  fontSize: FONT.fontSize.medium,
};
