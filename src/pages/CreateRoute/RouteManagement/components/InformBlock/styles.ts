import { SxProps, Theme } from '@mui/material';
import { FONT } from '@/constants/font';

export const informBlockStyles: SxProps<Theme> = {
  paddingTop: '20px',
};

export const titleStyles: SxProps<Theme> = {
  fontSize: FONT.fontSize.extraLarge,
  marginBottom: '16px',
};
