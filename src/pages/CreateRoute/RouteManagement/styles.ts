import { SxProps, Theme } from '@mui/material';
import { COLORS } from '@/constants/colors';

export const mapBlockStyles: SxProps<Theme> = {
  width: '1126px',
  paddingLeft: '20px',
  display: 'flex',
  justifyContent: 'space-between',
  backgroundColor: COLORS.white,
};
